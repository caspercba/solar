{
  "name": "solar-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/**/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
